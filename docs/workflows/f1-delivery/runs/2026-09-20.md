# Ejecución 2026-09-20 — issue #3

## Alcance autorizado y trazabilidad

Kalista autorizó completar F0 y construir F1, y después sustituir Cowboy/Gun por Bandit/Mint. PR siempre lista, sin merge, despliegue, registro de apps ni cuentas/bots/runners. Referencia de canales indicada por la operador: gilraennr; sin API/apps registradas.

Issue: https://github.com/tears-mysthrala/chat-overlay/issues/3. Base inspeccionada: origin/main, commit 02a85bc. Rama `feat/3-f1-overlay`; worktree `C:/Users/unaiu/.t3/worktrees/chat-overlay-worktrees/3-f1-overlay`. Checkout original preservado. Gates posteriores: https://github.com/tears-mysthrala/chat-overlay/issues/4.

## Decisiones y fallos conservados

- Auditoría inicial Cowboy/Gun: tres avisos en Hex 2.5.1. Cambio aprobado, árbol nuevo con 10 paquetes y 0 avisos Hex.
- Imagen de prueba Bookworm: 232 coincidencias Grype. Alpine: cuatro coincidencias. Se conserva fallo del gate, sin supresiones; no se declara release limpia.
- Adaptación a Mint/Bandit: se probaron HTTP real local, SSE y WebSocket fragmentado/ping. Se amplió el límite de conexiones del listener para admitir 100 SSE repartidos entre aceptores; Admission mantiene el máximo global de 100.
- La primera carga tenía deriva de temporización y cinco muestras fuera de su bootstrap. Se esperó al snapshot inicial y se cambió a plazos monotónicos; la prueba corregida entregó todas las muestras al ritmo objetivo.
- Una limpieza de procesos de test tenía carrera `alive?`/stop; se reemplazó por supervisión ExUnit. No era una caída del producto.
- Autoreview requirió codex.exe explícito por el shim Windows. Los hallazgos aceptados se corrigieron con regresiones: retención/replay, expulsión filtrada, orden de borrados, cooldown, handshake, página YouTube y Retry-After. No se atribuye una revisión humana al helper.

## Evidencia local

Comandos mantenidos en [procedimiento](../procedure.md). `mix check`, build Linux de validación, inventario/licencias, estático, Gitleaks, trazabilidad, smoke endurecido y navegador ejecutados. Los resultados detallados están en [verification.md](../../../verification.md). Artefactos grandes locales en output/; el workflow sube JSON a GitHub durante 14 días, sin imagen ni datos reales.

Carga medida de 60 s: 4.500 eventos, 45.000/45.000 entregas, cero errores, p95 49 ms; 10 perfiles, 30 fuentes demo, 100 lectores, 2 CPU/1 GiB. No acredita horas ni vivo.

## Pendientes de cierre

Revisión final: helper autoreview con Codex, modo local, `--json-output output/review-6.json --output output/review-6.txt`, codex.exe explícito; salida 0 y 0 hallazgos accionables. Se corrigieron 10 hallazgos aceptados en seis pasadas, con pruebas de regresión; no se usó un panel ni se cambió de motor. `mix check`: 38 pruebas PASS en Windows y Linux. No se atribuye prueba ejecutada al revisor: las ejecutó el agente principal.

Imagen local final: `sha256:bd7b81e2b50796070f8a6f420bc8df85651970f8ece2c6c653e40e51d6482a66`. CI y enlace de PR se registran tras abrirla. Cuatro hallazgos de imagen siguen abiertos. No se ejecutaron plataformas reales, OBS, prueba 4 h/24 h ni firma/distribución. No merge/despliegue/DNS/túnel. Estos gates permanecen en #4.
